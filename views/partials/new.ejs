<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="stylesheet" href="styles/main.css" />
  <title>Book Notes</title>
</head>
<body>
  
  <div>
      

          <div class="divSearch">
        <!-- Display the data from server -->
          <%= id %> <br/>
          <%= title %><br/>
          <%= author %><br/>
          <%= key %><br/>
          
          </div>

          <div>
            <form class="add" action="/add" method="post">
              <!-- placeholder will be equals to rating/review pass down from server check if it has value or not  -->
                <input type="text" name="myRating" value= "<%= locals.rating %>" placeholder="<%= locals.errorRating  ? errorRating: 'Rate from 1 to 10.' %>" autocomplete="off" autofocus="true" />
                <input type="text" name="myReview" value= "<%= locals.review %>" placeholder="<%= locals.errorReview  ? errorReview: 'Type your Review here.' %>" autocomplete="off" autofocus="true" />
                <button class="Add" type="submit" hidden>Add</button>
                <button class="Save" formaction="/save" type="submit" hidden>Save</button>
                <button class="Delete" formaction="/delete" type="submit" hidden>Delete</button>
            </form>
            <form action="/" method="get">
                <button type="submit">Cancel</button>
            </form>
        </div>

  </div>
<!-- if rating and review was received/pass down from the server -->
<!-- show the save and delete button -->
  <% if (locals.rating && locals.review) {%>
    <script>
      document.querySelector(".Save").removeAttribute("hidden")
      document.querySelector(".Delete").removeAttribute("hidden")
    </script>
  <% } %>
<!-- if rating and review was not received/ not pass down from the server -->
<!-- show the add button -->
  <% if (!locals.rating && !locals.review) {%>
    <script>
      document.querySelector(".Add").removeAttribute("hidden")
    </script>
  <% } %>
  
  <footer>Copyright © <%= new Date().getFullYear() %>
  </footer>
</body>
</html>